module.exports = {
  plugins: [
    require('autoprefixer'),
    require('postcss-custom-properties'), // Add this line to use the postcss-custom-properties plugin
    // Add other PostCSS plugins if needed
  ],
};
